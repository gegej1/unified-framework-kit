# longrun Seed

This directory is reserved for the future `longrun` seed export.

## Wave 1 status

Wave 1 creates the public placement skeleton only.

That means:

- this directory exists now
- the formal seed payload does not ship in this wave
- the existing root `CHECKLIST.md` remains only as a compatibility shim during the transition

## Intended role

When a later wave exports seed material, this directory is expected to hold reusable starter surfaces for project-memory setup and handoff.

Until then, use `../canonical/README.md` for the actual public explanation of the Project Memory Layer.
